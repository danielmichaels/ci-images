set -exuo

REGISTRY=danielmichaels
images=(
  "goose"
  "taskfile"
  "toolkit"
  "goa"
  )
ls
for i in "${images[@]}"; do
  docker build ./$i -t "${REGISTRY}/${i}" || exit
  docker push "${REGISTRY}/${i}" || exit
done

